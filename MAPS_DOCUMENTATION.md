# توثيق مشاكل الخرائط والحل الجذري (Maps Documentation)

## 1. ملخص المشكلة الأساسية
عند تشغيل شاشة الخريطة في بيئة **Expo Go** على الهواتف الفعلية، كانت الخريطة تظهر كـ **شاشة سوداء أو فارغة تماماً**، وكان المكون المسئول عن الخريطة وأدوات الرسم لا يستجيب أبداً.

---

## 2. الأسباب الجذرية التي تم تحديدها (Root Causes)

### أ. الاعتماد على `WebView` + `Leaflet.js` عبر CDN خارجي
1. **سياسة أمان المحتوى (CSP) و HTTP/HTTPS المختلط**: كان كود HTML المضمن يُحمّل مكتبة Leaflet من شبكات CDN خارجية (`unpkg.com` و `openstreetmap.org`). في إصدارات Android WebView الحديثة داخل Expo Go، يتم حظر مثل هذه الطلبات لحماية الجهاز.
2. **قيود `data:text/javascript;base64`**: محاولة تضمين محتوى Leaflet.js الضخم (أكثر من 200KB) كـ Base64 Data URI واجهت قيود أحجام السلاسل وإخفاقات في التفسير النحوي (Syntax Parsing) داخل بيئة Android WebView Native.
3. **أخطاء التفسير البرمجي النحوي (Inline JS Syntax Errors)**: معقدات تجميع السلاسل المضمنة في JavaScript نتج عنها أقواس زائدة `)` و `{` داخل سياق HTML String، مما أدى إلى توقف محرك JavaScript داخل WebView قبل الوصول إلى دالة تهيئة الخريطة `L.map()`.

---

## 3. الحل الجذري والنهائي (The Final Solution)

### التحويل إلى المكون الأصلي `react-native-maps`
بدلاً من استخدام WebView وHTML/Leaflet معقد، تم التحويل الشامل إلى المكتبة الأصلية **`react-native-maps`** المدمجة رسمياً مع Expo.

#### مميزات الحل الجديد:
1. **أداء ممتاز واستجابة فورية**: الخريطة تعمل عبر محرك الخرائط الأصلي بالجهاز (Native Map Engine) دون الحاجة لمتصفح مضمن.
2. **استقرار كامل في Expo Go**: لا توجد مشاكل CSP أو خوادم CDN أو ملفات HTML غير متوافقة.
3. **دعم كامل لكافة أدوات الرسم والقياس**:
   - إشارات الموقع (Markers / Waypoints)
   - رسم المضلعات والمساحات (Polygons)
   - رسم المسارات والخطوط (Polylines)
   - أداة قياس المسافات الفعالة بالـ Haversine Formula
   - تتبع مسار الحركة (Track Recording)
   - تبديل الطبقات (خرائط قياسية، قمر صناعي، طبوغرافية)
   - حفظ النقاط والمساحات في قاعدة بيانات SQLite المحلية.

---

## 4. الميزات الجديدة المضافة (Phase 2)

### أ. تسميات المواقع على الخريطة (Map Labels)
- **المناطق (Areas)**: تظهر فوقها `Label` بالاسم الذي يحدده المستخدم، بلون أزرق في خلفية داكنة.
- **النقاط (Waypoints)**: تظهر في بطاقة Callout عند الضغط عليها، تعرض الاسم والتصنيف.
- **العقارات (Properties)**: Callout يعرض الاسم + النوع + الحالة + السعر + الإحداثيات.

### ب. بطاقات التفاصيل الأنيقة (Detail Cards)
- **بطاقة عقار**: تعرض ملونة حسب الحالة (أزرق/أحمر/أخضر/برتقالي) مع أيقونات لـ:
  - نوع العقار (شقة/فيلا/أرض/مكتب/تجاري)
  - السعر بصيغة محملة
  - الحالة (للبيع/مباع/مؤجر/تحت المعالجة)
  - المساحة بالمتر المربع
  - العنوان
  - اسم المالك وهاتفه
  - الوصف
  - إحداثيات DMS
  - زر "فتح صفحة العقار" للتنقل لصفحة التفاصيل الكاملة
- **بطاقة نقطة**: تعرض اسم النقطة + التصنيف + التقييم (نجوم 1-5) + الوصف + إحداثيات DMS + زر "التقريب للنقطة".
- **بطاقة منطقة**: تعرض اسم المنطقة + التصنيف + المساحة بالهكتارات + المحيط + التقييم + الوصف.

### ج. قوالب الإدخال الغنية (Rich Input Forms)
نوافذ الحفظ (Save Waypoint/Area Modal) تحتوي على:
1. **اسم** (إلزامي)
2. **وصف** (اختياري)
3. **تصنيف** (Chips قابلة للاختيار):
   - للنقاط: عام، عقار، مكتب، معلم، عميل، زيارة موقع
   - للمناطق: عام، سكني، تجاري، زراعي، صناعي
4. **تقييم بالنجوم** (1-5 نجوم)
5. **عرض بصري للإحداثيات** (DMS) في الأعلى

نوافذ الحفظ **تمرير ScrollView** في حالة النماذج المتعددة الحقول، لتكون مريحة على الشاشات الصغيرة.

---

## 5. تحسينات المرحلة الثالثة (Phase 3) — حقول كاملة + Labels دائمة

### أ. نموذج إضافة نقطة عقار كاملة
تم تطوير نموذج "حفظ نقطة" ليصبح نموذج عقار شامل يحوي كل المعلومات التي يحتاجها مدير العقارات:

**معلومات الموقع**:
- إحداثيات DMS في الجزء العلوي للنموذج
- اسم العقار (مطلوب)

**معلومات المالك**:
- اسم المالك
- رقم الجوال
- وسائل تواصل أخرى (بريد، واتساب)

**تفاصيل الملكية**:
- وصف العقار
- تفاصيل إضافية (مرافق، مساحات خضراء، مميزات)
- قيمة العقار (ريال)
- المساحة (م²)
- تاريخ العرض

**الوسائط المرفقة**:
- نوع الوسائط: صور، فيديو، أو كلاهما
- عدد الملفات المرفقة

**التصنيف والتقييم**:
- شيبات تصنيف (chips)
- نجوم تقييم 1-5

### ب. Labels دائمة فوق كل عنصر
- **النقاط والحقوق العقارية**: تظهر بـ `Marker` ثانوي (بدون pin) فوق البين مباشرة مع تسمية الاسم في خلفية سوداء شفافة.
- **العقارات**: تظهر تسمية اسم العقار فوق الـ pin مباشرة.
- **المناطق**: تظهر تسمية وسط المضلع بـ Label أزرق.
- الهدف: عدم الحاجة للنقر على Map Element لمعرف اسمه — مجرد نظرة بصرية تكفي.

### ج. بطاقة واي بوينت تفصيلية موسّعة
- التصنيف، اسم المالك، رقم الجوال، وسائل اتصال أخرى
- قيمة العقار، المساحة، تاريخ العرض
- عدد ونوع الملفات المرفقة (صور/فيديو/كلاهما)
- تقييم بالنجوم
- وصف العقار + تفاصيل إضافية
- إحداثيات DMS

### د. ترحيل قاعدة البيانات (Migration)
تمت إضافة أعمدة جديدة آمنة إلى جدول `waypoints`:

| العمود | النوع | الوصف |
|---|---|---|
| `owner_name` | TEXT | اسم المالك |
| `owner_phone` | TEXT | رقم الجوال |
| `owner_contact` | TEXT | وسائل تواصل أخرى |
| `property_details` | TEXT | تفاصيل الملكية |
| `area_sqm` | REAL | المساحة |
| `price` | REAL | قيمة العقار |
| `listing_date` | TEXT | تاريخ العرض |
| `media_kind` | TEXT | نوع الوسائط (photo/video/both) |
| `media_count` | INTEGER | عدد الملفات |

دالة `safeMigrate()` تستخدم `ALTER TABLE ADD COLUMN` المغلف بـ try/catch للتعامل مع الأعمدة الموجودة بأمان.

---

## 6. ملفات إضافية تم تعديلها

- **src/database/db.ts**:
  - `safeMigrate()` يضيف أعمدة متعددة لـ `waypoints` و `areas`
  - `createWaypoint()` يحدّث توقيعه ليقبل كل الحقول الجديدة
- **src/screens/MapScreen.tsx**:
  - modal موسّع لكل بيانات العقار
  - trainers Labels دائمة فوق النقاط/العقارات
  - بطاقة تفاصيل موسعة

### د. قاعدة البيانات (Database Schema Migration)
- تمت إضافة أعمدة جديدة إلى جدول `waypoints` و`areas` عبر دالة `safeMigrate()`:
  - `category TEXT DEFAULT 'general'`
  - `tags TEXT DEFAULT '[]'`
  - `rating REAL DEFAULT 0`
- الترحيل آمن: يستخدم `ALTER TABLE ADD COLUMN` المغلف في try/catch للتعامل مع الأعمدة الموجودة.

---

## 5. الملفات المتأثرة والتعديلات

- **`src/screens/MapScreen.tsx`**: تم إعادة كتابته بالكامل باستخدام مكونات `<MapView>`, `<Marker>`, `<Polygon>`, `<Polyline>`, `<Circle>`, `<Callout>` من `react-native-maps` مع بطاقات التفاصيل وقوالب الإدخال الغنية.
- **`src/database/db.ts`**: تم إضافة `safeMigrate()` لإضافة أعمدة `category`, `tags`, `rating` إلى `waypoints` و`areas` بأمان.
- **`src/screens/map/buildMapHTML.ts` & `leaflet_data.ts`**: تم حذفهما بالكامل وتبسيط البنية.
- **`app.json`**: تم ضبط أذونات الموقع والأيقونة التكيفية بشكل صحيح.
- **`src/database/db.ts`**: تم تحديث توقيع `createWaypoint` و`createArea` لدعم الحقول الجديدة.

