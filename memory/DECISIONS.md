# القرارات المهمة — Decisions

سجل القرارات المؤثرة مع السبب والتاريخ. الأحدث أولاً.

---

## 2026-08-11 — اعتماد نظام الذاكرة وتتبع التعديلات
- **القرار**: إنشاء `memory/` + مهارة `project-memory` + خطاف `post-commit` لسجل
  التعديلات تلقائياً، وتحديث `AGENTS.md` (عام ومحلي) ببروتوكول الذاكرة.
- **السبب**: قدرة على تذكّر كل شيء ومراجعة كل التعديلات عبر git + الذاكرة الدائمة.

## 2026-08-11 — الرفع إلى GitHub كمستودع خاص
- **القرار**: إنشاء مستودع خاص `adibadm409-eng/property-manager-app` (فرع `main`)
  ورفع التطبيق كاملاً إليه، مع استثناء `node_modules` و `dist-bundle` و `metro.log`.
- **السبب**: نسخ احتياطي وتتبع نسخ عبر GitHub؛ التوثيق عبر GitHub CLI
  (`gh auth login` بطريقة المتصفح).

## 2026-08-11 — تمكين strictNullChecks في حزمة "open agent"
- **القرار**: إضافة `"strictNullChecks": true` إلى tsconfig الحزمة.
- **السبب**: بدونها يفشل تضييق النوع على الاتحاد `{ok:true}|{ok:false}` بعد فحص
  `res.ok` — كاد يخدعنا تشابه الكود مع نسخة التطبيق التي تعمل بـ strict.
- **الدرس**: فحص النتائج بالـ exit code الكامل لا بـ grep مُصفّى.

## 2026-08-16 — اعتماد عقد تطبيق نوته عقارية محلي وإعادة بناء المجال والوكيل
- **القرار**: يبقى التطبيق محلياً بالكامل دون مزامنة سحابية في هذه المرحلة، مع إعادة بناء طبقات المجال حول مشاريع متعددة الأنماط، ودفتر نقد وأقساط قائم على قيود ledger، ووكيل كيمو مقسم إلى Intent Router وSkill Registry وPlanner وTool Policy وDomain Services وVerifier وPresenter.
- **السبب**: المطلوب هو تجربة محلية قوية ومركزة، مع قدرة كيمو على تنظيم مشاريع أراضٍ ومبانٍ وأبراج وتوثيق الدفعات دون خلط بين البيانات أو الاعتماد على prompt وحده.
- **المرجع**: `docs/PRODUCT_CONTRACT_AR.md`.
- **قاعدة تنفيذ**: لا يذهب الاستيراد مباشرة إلى SQLite؛ يمر باكتشاف المخطط والتطبيع والمعاينة وكشف التكرار ثم commit ذري وتحقق لاحق. أدوات المجال المتخصصة تسبق CRUD العام.

## 2026-08-16 — كيمو كوكيل قائم على المهارات وخريطة شاشات التطبيق

- أضيف عقد صريح لدورة كيمو: فهم، تخطيط، سؤال، تنفيذ، تحقق، تعافٍ، وإكمال، مع `AgentPlan` و`AgentDecision` و`AgentSkill` وأحداث مرئية قابلة لإعادة العرض.
- أضيف سجل مهارات مستقل في `src/assistant/skills.ts` لاختيار مسارات تنظيم المشاريع، الدفعات، مراجعة السلامة، التقارير، البحث، والمساعد العام. تُمرر إرشادات المهارة المختارة ديناميكياً إلى دورة الوكيل، ولا تعتمد العملية على prompt ثابت وحده.
- أضيفت ذاكرة تشغيلية محلية `agent_runtime_events` وناشر أحداث يحفظ الخطة والمرحلة والمهارة والقرارات والملاحظات والتعافي، وتعيد شاشة المحادثة تحميلها عند فتح الجلسة.
- أضيفت لوحة مرئية في شاشة كيمو لعرض المرحلة والمهارة والخطة وعدّاد الإنجاز والقرارات والملاحظات، مع حالات سؤال وموافقة وإيقاف وتعافٍ.
- أضيفت `app_screen_catalog` وخريطة `src/agent/screenCatalog.ts` لربط كل شاشة بالكيانات وأدوات القراءة والكتابة والتحقق ومستوى الخطر وسياسة التعديل الآمن.
- أضيفت قيود تنفيذية تمنع تعديل المجاميع المالية عبر CRUD، وتفرض شروط الدفعات وحد الاستيراد، وتوقف الدوران بعد حدود زمنية وعددية وتكرار متطابق.
- اختبارات التحقق الحالية: TypeScript يمر، واختبارا مهارات كيمو وخريطة الشاشات يمران.

